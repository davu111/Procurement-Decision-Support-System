@startuml
!theme plain

actor "Quản trị viên" as Admin
boundary "Browser\n(Trang Nhân viên)" as Browser
boundary "ConfirmDialog\nComponent" as Dialog
control "EmployeeController\n(employee-service)" as Controller
database "Database\n(Employee)" as Database

Admin -> Browser: Truy cập trang\n"Nhân viên"
activate Browser

Browser -> Admin: Hiển thị danh sách\nnhân viên

Admin -> Browser: Nhấn nút "Hiệu hóa"\ntại người dùng vô hiệu

Browser -> Dialog: Mở hộp thoại\nxác nhận
activate Dialog

Dialog -> Admin: Hiển thị thông báo\n"Bạn có chắc chắn\nhiệu hóa người dùng này?"

alt Quản trị viên hủy
  Admin -> Dialog: Nhấn "Hủy"
  Dialog -> Dialog: Đóng hộp thoại
  deactivate Dialog
  
  note over Browser
    Không hiệu hóa người dùng
    Giữ nguyên trạng thái vô hiệu
  end note
else Quản trị viên xác nhận
  Admin -> Dialog: Nhấn "Xác nhận"
  
  Dialog -> Controller: PATCH /api\n/employees/active/{userId}
  activate Controller
  
  Controller -> Database: UPDATE employee\nSET status = 'ACTIVE'
  activate Database
  Database -> Database: Hiệu hóa\n người dùng
  Database --> Controller: Success
  deactivate Database
  
  Controller --> Dialog: HTTP 200 OK\n(Boolean)
  deactivate Controller
  
  Dialog -> Dialog: Đóng hộp thoại
  
  Dialog -> Browser: Refresh danh sách\n(GET /api/employees)
  activate Browser
  
  Browser -> Browser: Cập nhật danh sách
  Browser -> Admin: Hiển thị danh sách\nmới (người dùng đã hoạt động)
  deactivate Browser
end

@enduml
