services:
  supplier_mysql:
    image: mysql:8.0.36
    environment:
      MYSQL_ROOT_PASSWORD: mysql
    ports:
      - "3347:3306"
    volumes:
      - supplier_mysql_data:/var/lib/mysql

volumes:
  supplier_mysql_data: